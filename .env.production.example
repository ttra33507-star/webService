# Production env example for webService frontend
# Copy to `.env.production` in CI and replace values with production ones.
VITE_API_BASE=https://api.example.com
VITE_API_BASE_URL=https://api.example.com
VITE_APP_VERSION=2025.11.17-1

# Add other VITE_ variables here as needed for production.
